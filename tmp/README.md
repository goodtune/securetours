# tmp/

Scratch directory for throwaway artifacts.

Anything that scripts write while doing comparison work — playwright
screenshots, generated reports, ad-hoc captures — should land here.

`tmp/` is gitignored. Safe to delete the whole folder at any time.
